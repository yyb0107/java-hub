server:
  port: 8080
spring:
  application:
    name: service-provider-1
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9090/eureka
management:
  endpoints:
    web:
      exposure:
        include: "*"
